<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The Dev Blog</title>
    <link rel="stylesheet" type="text/css" href="/css/style.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css">
</head>
<nav class="navbar has-background-success" role="navigation" aria-label="main navigation" id="navBarBanner">

    <div id="navbarBasicExample" class="navbar-menu">
        <div class="navbar-start">
            <a href="/" class="navbar-item">
                Home
            </a>

            <a href="/blogs" class="navbar-item">
                Dashboard
            </a>

            {{#if logged_in}}
            <a href="/blogs/create" class="navbar-item">
                Create a Blog
            </a>
            {{/if}}
        </div>

        <div class="navbar-end">
            <div class="navbar-item">

                {{#if logged_in}}
                <div class="buttons">
                    <a id="logout" href="/logout" class="button is-warning" data-user-id="{{user_id}}">
                        Log out
                    </a>
                </div>
                {{else}}
                <div class="buttons">
                    <a href="/signup" class="button is-info">
                        <strong>Sign up</strong>
                    </a>
                    <a href="/login" class="button is-warning">
                        Log in
                    </a>
                </div>
                {{/if}}

            </div>
        </div>
</nav>

<div class="Mainpage">
    {{{body}}}
</div>
</main>
{{#if logged_in}}
<script src="/js/logout.js"></script>
{{/if}}

</html>